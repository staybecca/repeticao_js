// soma é iniciada com zero
// i é igual a zero
// i <= é menor ou igual 5
// i é incrementado de um em um
let soma = 0;
for (let i = 0; i <= 5; i++) {
    soma += i;  // Acumulando a soma
}
console.log(soma);  // Vai mostrar a soma total dos números de 0 a 5
